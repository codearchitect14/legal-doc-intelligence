# components

Shared design system components (buttons, nav bar, status badges, layout shells) built on the design tokens in `styles/`, per `PROJECT_PLAN_v2.md` Section 13.1.

**Inputs:** props only, no page-specific business logic.
**Outputs:** reusable UI building blocks used across `pages/` and `features/`.
