# features

Feature-specific components tied to a single domain area, e.g. the chronology timeline view, the draft review/export screen, the document viewer panel.

**Inputs:** case/document data from `lib/`.
**Outputs:** composed feature UI consumed by `pages/`.
