# lib

The typed API client (generated from the FastAPI OpenAPI schema per Section 14 Phase 6) and shared frontend utilities.

**Inputs:** backend OpenAPI schema, auth token state.
**Outputs:** typed request functions/hooks consumed by `pages/` and `features/`.
