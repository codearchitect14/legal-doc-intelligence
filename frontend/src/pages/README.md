# pages

Top-level routed pages: marketing site pages (home, product, solutions, security, pricing, demo request) and authenticated app pages (dashboard, case detail, chronology, draft review, firm settings), per `PROJECT_PLAN_v2.md` Section 13.

**Inputs:** route params, data from `lib/` API client hooks.
**Outputs:** rendered page composed from `components/` and `features/`.
