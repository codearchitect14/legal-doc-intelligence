# styles

Design tokens (color palette, typography, spacing scale, corner radius, shadow depth) and Tailwind configuration, per `PROJECT_PLAN_v2.md` Section 13.1. All components consume these tokens rather than redefining values ad hoc.
